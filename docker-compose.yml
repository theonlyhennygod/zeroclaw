# CrabClaw Docker Compose Example
# 
# Quick start:
#   1. Copy this file and set your API key
#   2. Run: docker-compose up -d
#   3. Access gateway at http://localhost:3000
#
# For more info: https://github.com/theonlyhennygod/crabclaw

services:
  crabclaw:
    image: ghcr.io/theonlyhennygod/crabclaw:latest
    # Or build locally:
    # build: .
    container_name: crabclaw
    restart: unless-stopped
    
    environment:
      # Required: Your LLM provider API key
      - API_KEY=${API_KEY:-}
      # Or use the prefixed version:
      # - ZEROCLAW_API_KEY=${ZEROCLAW_API_KEY:-}
      
      # Optional: LLM provider (default: openrouter)
      # Options: openrouter, openai, anthropic, ollama
      - PROVIDER=${PROVIDER:-openrouter}
      
      # Optional: Model override
      # - ZEROCLAW_MODEL=anthropic/claude-sonnet-4-20250514
      
    volumes:
      # Persist workspace and config
      - crabclaw-data:/data
      
    ports:
      # Gateway API port
      - "3000:3000"
    
    # Health check
    healthcheck:
      test: ["CMD", "crabclaw", "doctor"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes:
  crabclaw-data:
